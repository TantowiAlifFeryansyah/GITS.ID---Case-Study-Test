# Server
PORT=4000
NODE_ENV=development

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=publishing_db
DB_USER=postgres
DB_PASS=postgres

# Auth
JWT_SECRET=supersecretkey_change_me
JWT_EXPIRES_IN=1d
BCRYPT_SALT_ROUNDS=10
